import PhotoGrid from '../components/PhotoGrid';

function Photos() {
    return (
        <div className="photos-container">
            <PhotoGrid />
        </div>
    );
}

export default Photos; 